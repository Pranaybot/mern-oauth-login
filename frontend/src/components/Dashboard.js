import React from 'react'; // Import React to create components

// Define the Dashboard component
const Dashboard = () => {
  return (
    <div>
      <h1>Welcome to the Dashboard</h1>
      {/* This is where you can display user-specific information */}
    </div>
  );
};

export default Dashboard; // Export the Dashboard component as the default export
